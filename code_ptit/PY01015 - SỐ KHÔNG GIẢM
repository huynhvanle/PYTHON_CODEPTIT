from math import *
from collections import deque
def check(s):
    for i in range(len(s) - 1):
        if int(s[i]) > int(s[i + 1]):
            return 0
    return 1
for t in range(int(input())):
    s = input()
    if check(s):
        print("YES")
    else:
        print("NO")
