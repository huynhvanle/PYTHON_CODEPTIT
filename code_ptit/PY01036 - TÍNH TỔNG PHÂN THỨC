from math import *
for t in range(int(input())):
    n = int(input())
    st = 0
    if n % 2 == 0: st = 2
    else: st = 1
    sum = 0
    while st <= n:
        sum += 1/st
        st += 2
    print("%.6f" % sum)
