from math import *
for t in range(int(input())):
    s = input()
    s += " "
    rs = ""
    cnt = 1
    for i in range(1,len(s)):
        if s[i] == s[i - 1]:
            cnt += 1
        else:
            rs += str(cnt)
            rs += s[i - 1]
            cnt = 1
    print(rs)
