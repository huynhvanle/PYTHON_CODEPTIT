from math import *
from collections import deque
def check(s):
    for c in s:
        if c != '4' and c != '7':
            return ('NO')
    return 'YES'
for t in range(int(input())):
    s = input()
    print(check(s))
