from math import *
from collections import deque
for t in range(int(input())):
    n, x, m = map(float, input().split())
    cnt = 0
    while 1:
        n = n + n * x / 100
        cnt += 1
        if n >= m:
            break
    print(cnt)
